import { StyleSheet } from 'react-native';

export const StackStyle = StyleSheet.create({
  navigation: {
    paddingTop: 10,
    paddingBottom: 10,
    backgroundColor: 'black',
    flex: .07,
  }
})
