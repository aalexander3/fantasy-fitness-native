import { StyleSheet } from 'react-native';

export const StackStyle = StyleSheet.create({
  navigation: {
    paddingTop: 10,
    paddingBottom: 10,
    backgroundColor: '#fff',
    borderColor: '#fff',
    borderWidth: 0,
    flex: .07,
  }
})
