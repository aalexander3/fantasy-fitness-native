import { StyleSheet } from 'react-native';

export const ScrollStyle = StyleSheet.create({
  activeScroll: {
    height: 350,
    padding: 10,
  },
  scrollView: {
    height: 100,
    padding: 10,
  }
})
